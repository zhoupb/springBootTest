spring:
  profiles:
    active: prod
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost/girls?useUnicode=true&characterEncoding=utf-8&useSSL=false
    userName: root
    password: root
  jpa:

      show-sql: true


com.dudu:
      name: "嘟嘟duduMD"
      want: "祝大家鸡年大吉"
      double1: ${com.dudu.name}在此${com.dudu.want}